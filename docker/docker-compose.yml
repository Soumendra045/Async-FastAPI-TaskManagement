services:
  mysql:
    image: mysql:8.4
    container_name: mysql_db
    env_file:
      - ../.env/.env
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    
volumes:
  mysql-data: